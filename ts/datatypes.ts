/** @format */

let firstName: string;
firstName = "Almustapha";

let age: number;
age = 31;

let isPresent: boolean = false;

let indexNumber: Array<number>;

let anyarray: any[];

enum Color {
	RED,
	GREEN,
	WHITE,
}
let one: Color = Color.GREEN;

indexNumber = [2, 3, 5, 6];
anyarray = ["String", 22, true, Color.RED];
let names: string[] = ["Almustapha", "Tukur", "Umar"];

console.log("firstName", firstName.toUpperCase());
console.log("age", age);
console.log("isPresent", isPresent);
